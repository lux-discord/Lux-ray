{
  "name":"Lux-ray Discord bot",
  "description":"A Discord bot that help manage server",
  "keywords":[
    "Discord",
    "Python",
    "Disnake",
    "Bot"
  ],
  "repository":"https://github.com/Euxcbsks/Lux-ray",
  "logo":"https://raw.githubusercontent.com/Euxcbsks/Lux-ray/main/img/Lux-ray-bot.png?s=200",
  "env":{
    "PORT":{
      "description":"Which port should Flask run on"
    },
    "TOKEN_ALL":{
      "description":"Bot token use when run on both `DEV` and `PROD`(production) mode. (optional)\nWILL OVERWRITE OTHER TOKEN SETTINGS",
      "required":false
    },
    "TOKEN_DEV":{
      "description":"Bot token use when run on `DEV` mode. (optional)",
      "required":false
    },
    "TOKEN_PROD":{
      "description":"Bot token use when run on `PROD`(production) mode."
    },
    "DB_HOST_DEV":{
      "description":"Database host uri use when run on `DEV` mode. (optional)",
      "required":false
    },
    "DB_HOST_PROD":{
      "description":"Database host uri use when run on `PROD`(production) mode."
    },
    "DB_PORT_DEV": {
      "description":"Database host port use when run on `DEV` mode. (optional)",
      "required":false
    },
    "DB_PORT_PROD": {
      "description":"Database host port use when run on `PROD`(production) mode. (optional)",
      "required":false
    },
    "DB_TYPE_DEV":{
      "description":"Database type use when run on `DEV` mode. (optional)\nCurrent only support `mongodb` type.",
      "required":false
    },
    "DB_TYPE_PROD":{
      "description":"Database type use when run on `PROD`(production) mode.\nCurrent only support `mongodb` type."
    }
  },
  "buildpacks":[
    {
      "url":"heroku/python"
    }
  ]
}
